services:
  postgres:
    image: postgres:16
    container_name: streetvendor-postgres
    environment:
      POSTGRES_DB: street_vendor
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    container_name: streetvendor-redis
    ports:
      - "6379:6379"

volumes:
  postgres_data:
