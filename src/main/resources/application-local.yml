spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/street_vendor
    username: postgres
    password: postgres
  flyway:
    enabled: true
  jpa:
    hibernate:
      ddl-auto: validate

springdoc:
  api-docs:
    path: /api/docs
  swagger-ui:
    path: /swagger-ui/index.html
